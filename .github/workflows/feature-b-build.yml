name: feature-b-build

on:
  push:
    branches: [ "main" ]
    paths:
      - 'feature-b/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'feature-b/**'

obs:
  call-docker-build-and-push-workflow:
    uses: franktylerva/github-workflows/.github/workflows/docker-build-and-push.yml@main
    with:
      context: "./feature-b"
      image-tags: tfrank773/feature-b:1.0.1
    secrets: inherit